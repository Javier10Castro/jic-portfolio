{
  "name": "build-a-brief",
  "version": "1.0.0",
  "private": true,
  "description": "Javier Ibrahim — Portfolio & Build a Brief",
  "scripts": {
    "db:query": "node scripts/query.js",
    "db:repl": "node scripts/query.js --repl",
    "db:reset": "node scripts/db-reset.js --force",
    "db:migrate": "node scripts/run-migrations.js",
    "db:seed": "node scripts/seed-test.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "nodemailer": "^8.0.10",
    "pdfkit": "^0.18.0",
    "pg": "^8.21.0"
  }
}
